package io.compgen.sjq.server;

import io.compgen.cmdline.annotation.Command;

@Command(name="daemon", category="server", desc="Start an SJQ server in daemon mode (same options as 'server')")
public class DaemonStub {

	public void exec() {
		
	}
}
